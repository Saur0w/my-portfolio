import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

module.exports = {
  reactStrictMode: true,
  poweredByHeader: false, // Disable the Next.js footer (powered by Next.js)
};
